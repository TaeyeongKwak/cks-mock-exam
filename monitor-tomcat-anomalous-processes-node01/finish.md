The `tomcat` workload was monitored on `node01`, and anomalous process executions were recorded in the required report format.

This preserves the original runtime-detection objective without depending on a preinstalled Falco or Sysdig package.
