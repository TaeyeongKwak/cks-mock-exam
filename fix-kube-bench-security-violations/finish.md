Scenario complete.

The kube-bench style violations are fixed: the API server is using the required secure settings, the controlplane kubelet is hardened, and etcd no longer uses automatic TLS generation.
