Scenario complete.

The API server is secured again: anonymous access is blocked, `Node,RBAC` authorization is enabled, `NodeRestriction` is active, and authenticated access via `/etc/kubernetes/admin.conf` works.
